git_url("ssh://git@mydomain/folder/fastlane-match.git")

type("development") # The default type, can be: appstore, adhoc, enterprise or development

for_lane :beta do
	type "appstore"
end

# app_identifier(["tools.fastlane.app", "tools.fastlane.app2"])
username("fastlane@mydomain.com") # Your Apple Developer Portal username
